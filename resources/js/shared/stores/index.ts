/**
 * Shared Stores
 * 
 * This module exports all shared store configurations and instances.
 * Re-exports from the src/stores directory for backward compatibility.
 */

// Re-export store from src directory
export { store, Store, Dispatch, RootState } from '../../src/stores';
export { default } from '../../src/stores';
